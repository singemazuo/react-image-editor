

export const makeProductData = (code: string, image: string) => {
    return {
        id: code,
        preview: image,
        active: true,
    };
};

export const makeProductPartData = (code: string) => {

};